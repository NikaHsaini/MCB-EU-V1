module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'],
  theme: {
    extend: {
      colors: {
        ring: 'hsl(var(--ring))',
        background: 'hsl(var(--background))',
      }
    },
  },
  plugins: [],
}

